info:
  name: upload thumbnail
  type: http
  seq: 20

http:
  method: POST
  url: http://localhost:3000/api/courses/thumbnail
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
